<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  Checkstyle configuration for Advertisement Platform.
  This configuration aims to establish a consistent baseline that
  works well with Lombok and modern Java (21), without being overly
  strict. It is a starting point and can be tightened over time.
-->
<module name="Checker">
    <property name="severity" value="warning"/>

    <module name="TreeWalker">
        <!-- Naming conventions -->
        <module name="TypeName"/>
        <module name="MethodName"/>
        <module name="ParameterName"/>
        <module name="LocalVariableName"/>
        <module name="MemberName"/>

        <!-- Imports -->
        <module name="UnusedImports"/>
        <module name="RedundantImport"/>
        <module name="AvoidStarImport"/>

        <!-- Braces and blocks -->
        <module name="NeedBraces"/>
        <module name="LeftCurly">
            <property name="option" value="eol"/>
        </module>
        <module name="RightCurly"/>

        <!-- Whitespace and formatting -->
        <module name="WhitespaceAfter"/>
        <module name="WhitespaceAround"/>
        <module name="NoLineWrap"/>
        <module name="OperatorWrap"/>
        <module name="ParenPad"/>
        <module name="MethodParamPad"/>
        <module name="ArrayTrailingComma"/>

        <!-- Class design -->
        <module name="OneTopLevelClass"/>
        <module name="FinalClass"/>

        <!-- Javadoc: keep lenient for now (Task 10 will cover comprehensive JavaDoc) -->
        <module name="JavadocMethod">
            <property name="scope" value="public"/>
            <property name="allowMissingParamTags" value="true"/>
            <property name="allowMissingThrowsTags" value="true"/>
            <property name="allowMissingReturnTag" value="true"/>
            <property name="minLineCount" value="2"/>
            <property name="allowedAnnotations" value="Override, Test"/>
        </module>
        <module name="JavadocType">
            <property name="scope" value="public"/>
            <property name="excludeScope" value="package"/>
        </module>

        <!-- Annotations formatting -->
        <module name="AnnotationLocation"/>
        <module name="AnnotationUseStyle"/>
    </module>

    <!-- File length and header checks can be added later as project evolves -->
</module>
